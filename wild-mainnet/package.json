{
  "name": "wild-mainnet",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ wild-mainnet",
    "create-local": "graph create --node http://localhost:8020/ wild-mainnet",
    "remove-local": "graph remove --node http://localhost:8020/ wild-mainnet",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 wild-mainnet",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/client-cli": "^3.0.7",
    "@graphprotocol/graph-cli": "0.94.0",
    "@graphprotocol/graph-ts": "0.36.0"
  }
}
